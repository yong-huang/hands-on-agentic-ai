# 🤖 AI Agent 开发 30 小项目学习清单 · Todo List

> 通过 30 个小项目（每项目 50-300 行代码）系统掌握 AI Agent 开发核心技能
> 预计周期：6-7 周（每天有效学习 3-4 小时）
> 每个项目都配有 🤖 AI 提示词，复制发给 AI 即可获得完整代码
> 涵盖 LangChain 生态与 Agent Harness 架构双重视角

---

## 🤖 AI 辅助提示词速查

| 场景 | 提示词 |
|:---|:---|
| **开始一个新项目** | `我要开始 AI Agent 项目 [项目名称]，目标是 [项目目标]。请给我一个完整的、可运行的 Python .py 文件，约 [行数] 行，使用 LangChain/LangGraph 框架（如涉及 Harness 概念则结合 @harness-one/core），包含必要的环境配置、API 调用、错误处理和测试用例。只输出代码，不要解释。` |
| **代码报错** | `我在跑 AI Agent 代码时遇到这个报错：[粘贴报错信息]。请帮我修复，只输出修复后的完整代码。` |
| **Agent 不按预期工作** | `我的 Agent [项目名称] 没有按预期工作，[描述问题]。代码是：[粘贴关键代码]。请帮我诊断原因并给出修复方案。` |
| **工具调用失败** | `我的 Agent 工具调用没有正确执行，代码是：[粘贴关键代码]。请帮我修复工具定义或调用逻辑。` |
| **看不懂某段代码** | `请逐行解释这段 AI Agent 代码：[粘贴代码]。` |

---

## 📊 总进度

进度：████████████████████ 30/30 (100%)

| 阶段 | 项目数 | 已完成 |
|:---|:---:|:---:|
| 第一阶段：LLM 基础与 Prompt 工程 | 5 | 5 |
| 第二阶段：Agent Loop 与框架基础 | 5 | 5 |
| 第三阶段：工具系统 | 5 | 5 |
| 第四阶段：记忆系统与上下文工程 | 4 | 4 |
| 第五阶段：RAG 检索增强生成 | 4 | 4 |
| 第六阶段：多 Agent 协同 | 4 | 4 |
| 第七阶段：工程化、安全与可观测性 | 3 | 3 |
| **合计** | **30** | **30** |

---

## 🗂️ 第一阶段：LLM 基础与 Prompt 工程（项目 1-5）

> **目标**：理解大模型 API 的本质，掌握 Prompt 工程的核心技巧

---

### [x] 项目 1：Raw HTTP 调用 LLM API

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~60 |
| **核心知识点** | HTTP 请求、`chat/completions` 接口、JSON 解析 |
| **验收标准** | 用 `requests` 库直接调用 LLM API，返回正确响应 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Raw HTTP 调用 LLM API」，目标是用 HTTP 请求直接调用大模型 API，不使用任何 SDK。请给我一个完整的 Python .py 文件，约 60 行，使用 requests 库发送 POST 请求到 OpenAI/DeepSeek API，处理响应并打印结果。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 2：流式输出（SSE 打字机效果）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~70 |
| **核心知识点** | Server-Sent Events、流式响应解析、逐字输出 |
| **验收标准** | 实现 ChatGPT 风格的逐字打字机效果 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「流式输出」，目标是实现 ChatGPT 风格的逐字打字机效果。请给我一个完整的 Python .py 文件，约 70 行，使用 requests 的 stream=True 参数处理 SSE 流式响应，实时打印每个 token。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 3：Prompt 结构设计与角色定义

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~100 |
| **核心知识点** | System Prompt、Identity 设计、Few-shot、Chain-of-Thought、结构化输出、角色定义（IDENTITY.md） |
| **验收标准** | 让模型按指定格式输出 JSON；定义 2-3 种不同角色（工程师/产品经理/安全审计），对比同一问题的回答差异 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Prompt 结构设计与角色定义」，目标是设计不同角色的 Agent 并让模型按指定格式输出结构化数据。请给我一个完整的 Python .py 文件，约 100 行，设计 System Prompt + Few-shot 示例，使用 CoT 思维链引导模型输出 JSON 格式的结果；同时定义 2-3 种不同角色（工程师、产品经理、安全审计），用同一个问题测试不同角色的回答差异。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 4：Temperature 参数调优实验

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~70 |
| **核心知识点** | temperature、top_p、随机性控制 |
| **验收标准** | 对比 temperature=0 和 temperature=1 的输出差异 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Temperature 参数调优实验」，目标是对比不同 temperature 值对输出的影响。请给我一个完整的 Python .py 文件，约 70 行，用相同的 Prompt 分别以 temperature=0、0.5、1.0 调用 API，对比输出差异并总结。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 5：消息历史与多轮对话

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~90 |
| **核心知识点** | messages 数组、角色（system/user/assistant）、上下文保持 |
| **验收标准** | 实现一个简单的命令行多轮对话程序 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「消息历史与多轮对话」，目标是实现一个命令行多轮对话程序。请给我一个完整的 Python .py 文件，约 90 行，维护 messages 历史数组，支持用户连续提问，每次请求携带完整对话历史。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第二阶段：Agent Loop 与框架基础（项目 6-10）

> **目标**：理解 ReAct 模式与 Agent Loop，掌握框架基础与会话管理
>
> 注：原项目 8「Agent 推理可视化」已移除（事件流与项目 7 重复，可视化技能
> 而非 Agent 开发技能），8 号位由新项目「Plan-and-Execute：先规划后执行」
> 补齐——与 06/07 的 ReAct 交错推理形成架构对照，承接项目 30 的执行追踪。

---

### [x] 项目 6：ReAct 模式基础实现

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~140 |
| **核心知识点** | Reason + Act 交替、Thought/Observation 输出、Agent Loop 状态机 |
| **验收标准** | 实现「思考→行动→观察→继续思考」的循环 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「ReAct 模式基础实现」，目标是用代码实现 ReAct 推理循环。请给我一个完整的 Python .py 文件，约 140 行，不依赖 LangChain，手动实现 Thought→Action→Observation 循环，打印每一步的推理过程。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 7：Agent Loop 完整实现

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~160 |
| **核心知识点** | 最大步数限制、终止条件判断、中间结果累积、事件流捕获 |
| **验收标准** | Agent 能自主完成多步骤任务并在达到目标时停止 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Agent Loop 完整实现」，目标是实现完整的 Agent 推理循环。请给我一个完整的 Python .py 文件，约 160 行，包含最大步数限制（max_steps）、任务完成判断、中间结果缓存，当 Agent 认为自己完成时输出 Final Answer。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 8：Plan-and-Execute（先规划，后执行）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~200 |
| **核心知识点** | 任务分解、JSON 结构化计划、{{N}} 步骤引用替换、规划-执行-汇总三段架构 |
| **验收标准** | Agent 能把多步任务分解成 JSON 计划，按计划逐步执行工具（自动替换步骤引用），汇总给出答案 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Plan-and-Execute」，目标是实现先规划后执行的 Agent 架构。请给我一个完整的 Python .py 文件，约 200 行，Planner 用 LLM 把任务分解成 JSON 计划（工具白名单 + {{N}} 步骤引用），Executor 按计划逐步调用本地工具并替换引用，最后 Aggregator 汇总结果作答。包含 --demo 离线模式。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 9：使用 LangChain 创建 Agent

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 |
| **核心知识点** | LangChain Agent、Tool 类、AgentExecutor、本地工具封装（文件操作） |
| **验收标准** | 用 LangChain 实现一个带搜索功能的 Agent；Agent 能读取、写入、编辑本地文件 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「使用 LangChain 创建 Agent」，目标是使用 LangChain 框架创建 Agent 并封装本地文件操作工具。请给我一个完整的 Python .py 文件，约 120 行，使用 LangChain 的 Tool 类定义工具（含 read_file、write_file、list_dir），用 AgentExecutor 执行 ReAct Agent，包含安全路径约束。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 10：会话管理与状态持久化

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 |
| **核心知识点** | AgentSession、状态持久化、跨会话恢复、AgentStateStore |
| **验收标准** | 启动 Agent 完成对话，保存会话状态到本地，重启后从保存的状态恢复并继续对话 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「会话管理与状态持久化」，目标是实现 Agent 会话的保存与恢复。请给我一个完整的 Python .py 文件，约 120 行，创建 Agent 并进行多轮对话，保存会话状态（消息历史、工具调用记录等）到本地 JSON 文件，重启程序后从保存的状态恢复并继续对话。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第三阶段：工具系统（项目 11-15）

> **目标**：让 LLM 从「只会聊天」升级为「能动手做事」，掌握工具注册、调度、MCP 与授权机制

---

### [x] 项目 11：Function Calling 基础

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~100 |
| **核心知识点** | tools 定义、function calling、参数解析、Tool 定义与参数 Schema |
| **验收标准** | 模型能正确调用「获取天气」工具并返回结果 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Function Calling 基础」，目标是让模型能够调用天气查询工具。请给我一个完整的 Python .py 文件，约 100 行，定义 get_weather(city) 工具函数，用 tools 参数注册到 API 调用中，模型输出 tool_calls 后本地执行并返回结果。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 12：多工具注册与调度（工具注册表）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~140 |
| **核心知识点** | 多个工具注册、工具选择、参数映射、工具注册表（Registry）、ResourceDefinition、类型分发 |
| **验收标准** | Agent 能在「计算器」「搜索」「天气」中正确选择工具；实现按类型分发的统一调用接口 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「多工具注册与调度」，目标是注册多个工具让模型自动选择，并实现 Registry 分发系统。请给我一个完整的 Python .py 文件，约 140 行，注册 Calculator、Search、Weather 三个工具，实现 Registry 类（Map<resourceType, ResourceDefinition>），通过统一接口 dispatch(resourceType, operation, input) 调用，模型根据用户问题自动选择合适的工具。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 13：工具调用错误处理与重试

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 |
| **核心知识点** | 异常捕获、错误反馈、优雅降级、大结果卸载与占位符替换 |
| **验收标准** | 工具执行失败时，Agent 能向用户说明并尝试替代方案；大结果（>80K 字符）自动卸载为占位符 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「工具调用错误处理与重试」，目标是让 Agent 能优雅处理工具执行失败和大结果。请给我一个完整的 Python .py 文件，约 120 行，模拟某个工具（如 API 超时）失败，Agent 捕获异常后将错误信息反馈给模型，模型给出替代方案；同时实现工具大结果的自动卸载机制（超过阈值时替换为占位符）。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 14：MCP 协议集成

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~160 |
| **核心知识点** | MCP Client/Server、JSON-RPC 2.0、工具发现、MCP Server 调用、MCP 协议集成 |
| **验收标准** | Agent 能连接 MCP Server，自动发现并调用其提供的工具（如 filesystem MCP Server 的文件读取） |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「MCP 协议集成」，目标是让 Agent 接入 MCP 生态。请给我一个完整的 Python .py 文件，约 160 行，实现 MCP Client 连接 Server（如 filesystem MCP Server），完成初始化、工具发现和工具调用流程，Agent 能调用 MCP Server 提供的工具并返回结果。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 15：工具授权与 HITL 审批

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~130 |
| **核心知识点** | 工具审批、HITL（Human-in-the-loop）、危险操作拦截、命令白名单、安全约束 |
| **验收标准** | 危险工具（如删除文件）执行前需要用户确认，记录审批状态变化；非白名单工具被拒绝执行 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「工具授权与 HITL 审批」，目标是实现 Human-in-the-loop 的工具审批机制。请给我一个完整的 Python .py 文件，约 130 行，定义危险工具（如 delete_file），配置审批策略和白名单，Agent 调用危险工具时暂停等待用户确认，用户确认后才执行，记录审批状态变化。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第四阶段：记忆系统与上下文工程（项目 16-19）

> **目标**：让 Agent 具备跨会话的长期记忆和上下文管理能力

---

### [x] 项目 16：上下文构建与注入

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~110 |
| **核心知识点** | 系统提示词构建、上下文注入、BuildSystemPrompt、Identity + Memory + Workspace + Tools + Rules 组装 |
| **验收标准** | 展示 System Prompt 的完整构建过程：Identity + Memory + 工具列表 + 规则文件如何组合成最终 System Prompt |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「上下文构建与注入」，目标是理解 System Prompt 的完整构建过程。请给我一个完整的 Python .py 文件，约 110 行，实现 buildSystemPrompt 过程，展示 Identity（角色定义）、Memory（记忆信息）、工具列表、规则文件如何组合成最终的 System Prompt，打印各部分占比。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 17：对话记忆与长期记忆

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~150 |
| **核心知识点** | 消息历史管理、滑动窗口、截断策略、Embedding、向量数据库、相似度检索、MEMORY.md 沉淀 |
| **验收标准** | Agent 能记住之前对话的内容（短期记忆）；Agent 能从长期记忆（向量存储/MEMORY.md）中检索相关信息，跨会话使用记忆 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「对话记忆与长期记忆」，目标是让 Agent 具备短期和长期记忆能力。请给我一个完整的 Python .py 文件，约 150 行，实现短期记忆（消息历史管理、滑动窗口截断）和长期记忆（使用 Chroma 或 FAISS 向量数据库存储记忆，新对话开始时检索相关历史记忆注入 System Prompt），Agent 在多轮对话中自动提取重要信息并沉淀到长期记忆。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 18：上下文压缩策略

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~150 |
| **核心知识点** | Token 计数、截断策略、LLM 摘要、对话压缩（Compaction）、压缩策略配置、信息保留率评估 |
| **验收标准** | 长对话时自动压缩历史，不超出上下文窗口；对比不同压缩策略（按轮次/按 token 数）下上下文大小和信息保留率的差异 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「上下文压缩策略」，目标是自动管理长对话的上下文。请给我一个完整的 Python .py 文件，约 150 行，使用 tiktoken 计算 Token 数，配置不同的压缩策略（按轮次截断或按 token 数阈值），当超出阈值时自动执行截断或用 LLM 生成摘要，对比不同策略下压缩效果和信息保留率。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 19：记忆更新策略

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~130 |
| **核心知识点** | LLM 驱动的记忆更新、重要性判定、融合策略、事实提取与沉淀 |
| **验收标准** | Agent 能自主决定哪些信息需要存入长期记忆 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「记忆更新策略」，目标是让 Agent 自主决定记忆内容。请给我一个完整的 Python .py 文件，约 130 行，每轮对话结束后用 LLM 判断是否有值得长期记忆的信息（重要性判定），如有则调用 Embedding 存入向量库，支持记忆的融合与去重。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第五阶段：RAG 检索增强生成（项目 20-23）

> **目标**：让 Agent 能够检索外部知识库，实现 Agentic RAG

---

### [x] 项目 20：文档加载与切分

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~100 |
| **核心知识点** | 文档加载器、文本分割（RecursiveCharacterTextSplitter） |
| **验收标准** | 成功加载 PDF/TXT 并按语义切分 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「文档加载与切分」，目标是加载文档并切分为合适的 chunk。请给我一个完整的 Python .py 文件，约 100 行，使用 LangChain 的文档加载器加载 PDF/TXT，用 RecursiveCharacterTextSplitter 按语义切分，打印切分后的 chunk 数量和大小。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 21：向量化存储（Embedding + Chroma）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~120 |
| **核心知识点** | Embedding 模型、Chroma/FAISS、向量检索 |
| **验收标准** | 存入文档后，能用查询检索到最相关的 chunks |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「向量化存储」，目标是构建本地知识库。请给我一个完整的 Python .py 文件，约 120 行，使用 OpenAI/本地 Embedding 模型将文档 chunks 向量化，存入 Chroma 向量库，实现相似度检索并返回最相关的 3 个 chunks。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 22：Agentic RAG（检索+生成）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~150 |
| **核心知识点** | 检索工具、上下文注入、知识库问答 |
| **验收标准** | Agent 能基于知识库回答用户问题 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Agentic RAG」，目标是让 Agent 基于知识库回答问题。请给我一个完整的 Python .py 文件，约 150 行，实现一个 RAG 工具，Agent 根据问题决定是否检索，检索到的内容注入 System Prompt，生成最终答案。支持「我不知道」的拒绝回答。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 23：RAG 重排序与优化

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~140 |
| **核心知识点** | 重排序模型、MMR、检索质量优化 |
| **验收标准** | 重排序后检索精度提升 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「RAG 重排序与优化」，目标是提升检索质量。请给我一个完整的 Python .py 文件，约 140 行，在向量检索后使用 Rerank 模型（如 Cohere/本地 cross-encoder）对结果重排序，对比重排序前后的检索效果差异。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第六阶段：多 Agent 协同（项目 24-27）

> **目标**：掌握多 Agent 协作模式、工作流编排与主流框架

---

### [x] 项目 24：LangGraph 状态管理

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~150 |
| **核心知识点** | LangGraph、StateGraph、节点/边定义 |
| **验收标准** | 用 LangGraph 构建一个有状态的多步工作流 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「LangGraph 状态管理」，目标是使用 LangGraph 构建有状态工作流。请给我一个完整的 Python .py 文件，约 150 行，定义 StateGraph、多个节点（如 preprocess→analyze→summarize），实现状态在节点间的传递。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 25：多 Agent 协作（Manager-Worker 模式）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~180 |
| **核心知识点** | 任务分解、分工协作、结果合并、子 Agent 委派、Agent 间通信 |
| **验收标准** | Manager Agent 分解任务，多个 Worker 并行执行；创建主 Agent 和子 Agent（事实提取、风险评估），父 Agent 委派任务给子 Agent |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「多 Agent 协作」，目标是实现 Manager-Worker 模式和子 Agent 委派。请给我一个完整的 Python .py 文件，约 180 行，Manager Agent 负责分解任务并委派给子 Agent（Worker 1 搜索、Worker 2 分析），子 Agent 并行执行，Manager 汇总结果生成最终输出。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 26：工作流编排（Fan-out/Fan-in + 反思评审）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~180 |
| **核心知识点** | Fan-out/Fan-in 并行执行、结果聚合、Agent 反思（Reflection）、评审工作流、自我修正 |
| **验收标准** | 工作流同时扇出到 3 个子 Agent 并行执行后聚合结果；实现「写代码 → 代码评审 → 改进」的反思工作流，展示迭代改进效果 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「工作流编排 Fan-out/Fan-in + 反思评审」，目标是实现并行子 Agent 调用、结果聚合和反思迭代。请给我一个完整的 Python .py 文件，约 180 行，创建 Workflow：Fan-out 阶段同时调用 3 个子 Agent（安全审查、文档审查、测试审查），Fan-in 阶段聚合结果；同时实现反思工作流（写代码 Agent → 评审 Agent → 改进 Agent），循环执行直到评审通过。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 27：多智能体辩论/投票系统

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~180 |
| **核心知识点** | 专家角色、投票机制、共识达成 |
| **验收标准** | 多个专家 Agent 对同一问题进行讨论并达成共识 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「多智能体辩论系统」，目标是让多个专家 Agent 讨论并达成共识。请给我一个完整的 Python .py 文件，约 180 行，创建 3 个不同角色（架构师、安全工程师、产品经理）的 Agent，对一个设计方案进行讨论，最终投票决定最佳方案。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 🗂️ 第七阶段：工程化、安全与可观测性（项目 28-30）

> **目标**：让 Agent 从「Demo」走向「产品」，掌握安全防护、可观测性与评估能力

---

### [x] 项目 28：Agent HTTP 服务化

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~160 |
| **核心知识点** | FastAPI、SSE 流式传输、会话管理 |
| **验收标准** | 浏览器能通过 HTTP 与 Agent 流式对话 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Agent HTTP 服务化」，目标是让 Agent 成为一个可调用的 Web 服务。请给我一个完整的 Python .py 文件，约 160 行，使用 FastAPI 封装 Agent，支持 SSE 流式输出，客户端通过 `/chat` 端点发送消息并接收流式响应。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 29：Agent 安全防护（注入检测 + 内容过滤 + 沙箱）

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~180 |
| **核心知识点** | Prompt Injection 检测、内容过滤、Fail-closed 策略、工具白名单、沙箱隔离、危险操作拦截 |
| **验收标准** | Agent 拒绝执行危险命令，检测恶意注入，危险操作需人工确认；内容过滤对不安全输出直接拒绝 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Agent 安全防护」，目标是实现完整的安全防护体系。请给我一个完整的 Python .py 文件，约 180 行，实现三个层面：①输入安全——Prompt Injection 检测（如「忽略之前所有指令」）；②输出安全——内容过滤与 Fail-closed 策略；③执行安全——工具白名单、沙箱隔离（Docker 或工作区隔离），危险操作（如 rm -rf）触发 Human-in-the-loop 确认。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

### [x] 项目 30：Agent 可观测性与评估框架

| 项目信息 | 详情 |
|:---|:---|
| **行数** | ~200 |
| **核心知识点** | OpenTelemetry、Trace/Span、Token 统计、首 Token 延迟、性能指标监控、评估数据集、评分指标、自动优化闭环 |
| **验收标准** | Agent 每次执行生成完整 Trace 和指标；构建评估数据集运行 10+ 测试用例输出通过率；基于评估结果展示优化迭代效果 |

**🤖 开始提示词**：
> `我要开始 AI Agent 项目「Agent 可观测性与评估框架」，目标是让 Agent 的执行透明可追溯，并建立自动化评估能力。请给我一个完整的 Python .py 文件，约 200 行，实现三个模块：①可观测性——使用 OpenTelemetry 记录 Trace（包含 LLM 调用、工具调用等 Span），统计 Token 消耗、TTFT 和总执行时间；②性能监控——记录每次 Agent 运行的关键性能指标；③评估框架——创建评估数据集（10+ 测试用例），定义评分指标（准确率、完成率、工具调用正确率），运行评估并生成报告，展示多轮优化的改进效果。只输出代码。`

**完成日期**：________
**踩坑记录**：________

---

## 📅 周计划

| 周次 | 内容 | 项目数 |
|:---|:---|:---:|
| **第 1 周** | 项目 1-5（LLM 基础与 Prompt） | 5 |
| **第 2 周** | 项目 6-10（Agent Loop 与框架基础） | 5 |
| **第 3 周** | 项目 11-15（工具系统） | 5 |
| **第 4 周** | 项目 16-19（记忆与上下文） | 4 |
| **第 5 周** | 项目 20-23（RAG） | 4 |
| **第 6 周** | 项目 24-27（多 Agent 协同） | 4 |
| **第 7 周** | 项目 28-30（工程化、安全与可观测性） | 3 |

---

## 🏆 里程碑

- [x] **完成项目 1-5** → 掌握 LLM API 和 Prompt 工程
- [x] **完成项目 6-10** → 掌握 Agent Loop 与框架基础
- [x] **完成项目 11-15** → 掌握工具系统（Function Calling、MCP、授权）
- [x] **完成项目 16-19** → 掌握记忆与上下文管理
- [x] **完成项目 20-23** → 掌握 RAG 技术
- [x] **完成项目 24-27** → 掌握多 Agent 协同与工作流编排
- [x] **完成项目 28-30** → 具备 Agent 工程化、安全防护与可观测性能力

---

## 📝 每日日志

| 日期 | 项目 | 耗时 | 收获 | 踩坑 |
|:---|:---|:---:|:---|:---|
| | | | | |
| | | | | |
| | | | | |

---

## 🔧 环境配置

```bash
# 创建虚拟环境
conda create -n agent_dev python=3.11
conda activate agent_dev

# 安装核心依赖
pip install openai langchain langgraph
pip install chromadb faiss-cpu tiktoken
pip install fastapi uvicorn sse-starlette
pip install requests beautifulsoup4
pip install pypdf python-dotenv
pip install opentelemetry-api opentelemetry-sdk

# 可选：Harness 框架（项目涉及 Harness 概念时使用）
pip install harness-one
# 或从源码构建：git clone https://github.com/mastersof-ai/harness.git && pip install -e .

# 安装可选依赖
pip install torch transformers  # 本地 Embedding
pip install matplotlib networkx # 可视化
pip install streamlit            # Web UI
```
