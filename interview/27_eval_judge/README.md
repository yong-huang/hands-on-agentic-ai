# 27 · Agent 评测 Harness + LLM-as-Judge

> 覆盖面试题：Agent 评估框架有哪些？LLM-as-Judge 怎么设计？有什么偏见？
> 20 任务双通道：可判定集（规则评分器 vs LLM-Judge 0/1，测一致率）+
> 开放集（仅 LLM-Judge，带 rubric 1-5 分）；偏见实验对同一答案换身份
> 标注（GPT-4 vs 7B 小模型）与换位置（A/B 对比顺序）。

## 1. 运行与实测

```bash
MOCK=1 python eval_judge.py    # 离线: 预置回答与评分 (偏见两种均复现)
python eval_judge.py           # 真实: qwen3.8 当 Judge (5 判定 + 2 开放 + 4 探针)
```

真机实测（2026-09-09）：

| 指标 | 结果 |
|:--|:--|
| Judge 与规则评分一致率 | **5/5 = 100%**（验收 ≥80% ✓） |
| 开放集 rubric 均值 | 4.0 |
| 身份偏差（GPT-4 vs 小模型） | 分差 0.0 · 未复现 |
| 位置偏差（A/B 换序） | 两次都选更优答案 · 未复现 |

两个反直觉发现：(1) qwen3.8 当 Judge 与规则完全一致——可判定任务上
Judge 是可靠的；(2) 两个经典偏见在真机 Judge 上都未复现（身份标注与
顺序不影响判断）——**偏见主要存在于小模型 Judge / 长列表对比场景**，
把"偏见存在"背成教条不如实测自己的 Judge。离线模式复现了两种偏差
（4.6 vs 3.4；A/B 换序翻转），验证了实验设计的可检测性。

## 2. 深入要点

- 双通道：能规则判定的绝不劳烦 Judge；Judge 只处理开放集且必须带
  rubric（维度+分值定义），否则打分不可复现。
- 已知偏见要实测：身份偏差/位置偏差/冗长偏差——拿同一答案换标签即可
  复现或排除，这本身就是一套"评测自己的评测器"的方法。
- 缓解：匿名化来源、随机化位置、多次取样取均值；关键评测仍以规则/
  人工为准，Judge 分数只做排序不做绝对结论。

## 3. 文件结构

```
interview/27_eval_judge/
├── README.md            # 本篇
└── eval_judge.py        # 双通道评分 + 偏见探针（约 230 行）
```
