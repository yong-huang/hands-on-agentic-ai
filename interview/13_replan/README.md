# 13 · 规划失败检测与重规划

> 覆盖面试题：怎么处理规划失败？
> 团建组织管线注入 3 类失败各 1 次：①工具瞬断（场地查询 504）→ 重试；
> ②前提失效（20 座大巴无货）→ 改路（改订 2 辆 10 座）；③规划错误
> （餐厅预算 3000 超剩余额度 2000）→ 全局重规划。每步有断言式校验器，
> 失败按错误签名分类到 RETRY/ALTERNATIVE/REPLAN。真机对照 LLM 分类器。

## 1. 运行与实测

```bash
MOCK=1 python replan.py    # 离线: 注入故障全规则处置
python replan.py           # 真实: 追加 LLM 失败分类对照
```

实测（2026-09-09）：三类失败分别走对 重试/改路/重规划，管线最终完成 ✓；
真机 LLM 分类器对 3 个失败样例与规则判定 3/3 一致（504→RETRY、
无货→ALTERNATIVE、超额→REPLAN）。

决策轨迹（节选）：

```
[step 2] 订场地 -> ERROR: 504  ✗ 分类=RETRY
         ↳ 响应[重试] 第 2 次尝试 -> 湖畔厅预订成功 ¥1200 ✓
[step 3] 订 20 座大巴 -> ERROR: 无可用车型 ✗ 分类=ALTERNATIVE
         ↳ 响应[改路] -> 10 座中巴 ×2 ¥800 ✓
[step 4] 订餐厅 ¥3000 -> WARNING 超剩余额度 ✗ 分类=REPLAN
         ↳ 响应[重规划] 剩余计划改为 餐厅¥1800 -> 预算重算 ✓
```

## 2. 面试要点

- 先校验再行动：断言式校验器分两层——结果形状（ERROR/正常）+ 语义
  （表面成功但违反预算约束）。第三类失败最隐蔽，专抓"假成功"。
- 失败分类决定响应：一律重试会把错误计划执行三遍；前提失效要换路，
  规划本身错要重排。
- 重规划 ≠ 从头再来：只重排受影响的剩余子目标，已完成结果复用
  （与项目 31 断点续跑同一思想）。

## 3. 文件结构

```
interview/13_replan/
├── README.md            # 本篇
└── replan.py            # 校验器 + 失败分类 + 三种响应 + LLM 对照（约 220 行）
```
