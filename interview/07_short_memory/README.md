# 07 · 短期记忆管理器（窗口 + 摘要压缩）

> 覆盖面试题：上下文窗口溢出怎么办？摘要压缩和滑动窗口各有什么问题？
> 20 轮长对话，前几轮埋 5 个关键事实（姓名/城市/截止日/预算/咖啡偏好），
> 两种超限策略对跑：滑动窗口（丢最旧）vs 摘要压缩（LLM 归纳旧对话）。
> 第 20 轮提问 5 个事实，统计保留率。

## 1. 运行与实测

```bash
MOCK=1 python short_memory.py    # 离线: 窗口机械执行 + 摘要走预置
python short_memory.py           # 真实: qwen3.8 做摘要与最终问答
```

实测（2026-09-09，预算 350 est.tokens，人为收紧使 20 轮必然多次溢出；
窗口截断 5 次 / 摘要压缩 1 次均真实触发）：

| 事实 | 滑动窗口 | 摘要压缩(真机) |
|:--|:--:|:--:|
| 姓名(T1) | ✗ | ✓ |
| 城市(T1) | ✗ | ✓ |
| 截止日(T2) | ✗ | ✓ |
| 预算(T2) | ✗ | ✓ |
| 咖啡(T16) | ✓ | ✓ |
| **保留率** | **1/5** | **5/5** |

真机 qwen3.8 的摘要把预算细节也保留了（提示词要求"必须保留数字"）——
摘要压缩的下限很高，但代价是每次压缩一次 LLM 调用，且失败模式从
"确定性丢失"变成"不可预测的漏记"。两种策略各自丢失场景：窗口确定性
丢早期（实测保留率 1/5），摘要丢细节（离线预置摘要抹掉预算后 4/5 可复现）。

## 2. 深入要点

- 滑动窗口：零成本、可预测，但"最近的才存在"，早期事实整段蒸发。
- 摘要压缩：保留语义骨架，细节（数字/日期/专名）最易被抹平，且引入
  额外 LLM 调用与延迟。
- 生产组合拳：窗口保近期原文 + 摘要保远期骨架 + 关键事实抽取进长期
  记忆库（项目 8）按需检索——三层各管一段。

## 3. 文件结构

```
interview/07_short_memory/
├── README.md            # 本篇
└── short_memory.py      # 窗口/摘要双策略 + 保留率评测（约 220 行）
```
